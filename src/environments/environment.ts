export const environment = {
  production: true,
  //apiUrl: 'http://localhost:3000/api' // Ajusta esta URL para que que funcione correctamente con Render
  apiUrl: 'https://jwhistoryback.onrender.com/api' // Ajusta esta URL para que que funcione correctamente con Render
};
